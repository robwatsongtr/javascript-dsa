/* 
Write a function called minSubArrayLen which accepts two parameters -
an array of positive integers and a positive integer.

This function should return the minimal length of a _contiguous_
subarray of which the sum is greater than or equal to the integer
passed into the function. If there isn't one, return 0 instead. 

Examples: 

minSubArrayLen([2,3,1,2,4,3], 7) // 2 -> because [4,3] is smallest subarray


*/

function minSubArrayLen(arr, num) {
  
}